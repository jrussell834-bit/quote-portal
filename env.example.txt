# Copy this file to .env and fill in your values
# Database Configuration
# Option 1: Use DATABASE_URL (recommended for Railway/cloud)
DATABASE_URL=postgres://user:password@host:port/database
PGSSL=true

# Option 2: Use individual connection parameters (for local dev)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=quoteportal
# PGSSL=false

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=dev-secret-change-me-in-production

# Email Configuration (optional - for reminder emails)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_SECURE=false
# SMTP_FROM=noreply@yourdomain.com

# Server Port
PORT=4000

# Frontend API URL (for production builds)
# VITE_API_URL=http://localhost:4000/api
